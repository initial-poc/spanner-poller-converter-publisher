apiVersion: v1
kind: Service
metadata:
  name: pnr-resequencer-service
  labels:
    app: pnr-resequencer-service
spec:
  ports:
    - name: "pnr-resequencer-service"
      port: 9000
  selector:
    app: pnr-resequencer-service-pod